// declaración.

function example(a, b, c){
    return a+b+c
    }

// Transformando esta declaración de función a una función de expresión.

const expresion = function(a, b, c) {
    return a + b + c;
};
